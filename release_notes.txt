━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  DQ Admin  |  v1.0.2+26  |  TESTING BUILD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  This build is for internal testing only.

─────────────────────────────────────
 CHANGES IN THIS BUILD
─────────────────────────────────────
• Removed global "Products" chip from dashboard
  Products are now only accessible inside a store:
  Stores → Store Detail → Manage Products

• Dashboard Quick Access redesigned (5 items, 2+2+1 layout)
  - Stores is now the first and primary entry point
  - "Stores" subtitle updated: "Manage locations & products"
  - Staff moved to a full-width tile at the bottom

• Top Stores cards are now tappable
  Tapping a store in the Top Stores section navigates
  directly to that store's detail page

─────────────────────────────────────
 WHAT TO TEST
─────────────────────────────────────
□ Dashboard loads with 5 Quick Access items (no Products chip)
  → Stores, Orders, Analytics, Invite Staff, Staff

□ Tap "Stores" → goes to Stores list
  → Tap a store → goes to Store Detail
  → Tap "Manage Products" → goes to Products page for that store

□ Top Stores section (if stores have orders)
  → Tap a store card → should navigate to that store's detail page

□ Staff tile (bottom row, full-width)
  → Should render cleanly with no empty space next to it

□ Back navigation from Store Detail → Dashboard
  → Should work correctly

─────────────────────────────────────
 KNOWN SCOPE
─────────────────────────────────────
• No backend changes in this build
• Column mapping persistence (saving per store) coming next

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
