━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  DQ Admin  |  v1.0.2+24  |  TESTING BUILD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  This build is for internal testing only.

─────────────────────────────────────
 BUG FIXES IN THIS BUILD
─────────────────────────────────────
• Bulk upload now correctly reads "Opening" stock column
  from StockReport-Export files (was silently saving 0 stock)
• Added support for more stock column names:
  Opening, Closing, Qty, Quantity, Available, Stock on Hand, etc.
• Upload result now shows a clear warning when nothing was saved
  (previously showed "Upload Successful!" even for 0 products)
• Backend: products with 0 stock now saved (were silently skipped)
• Backend: admin product list shows all products including 0-stock items

─────────────────────────────────────
 WHAT TO TEST
─────────────────────────────────────
• Upload StockReport-Export (1).xlsx → should show products created
• Manage Products screen should show all uploaded products
• Upload a file with wrong columns → should show clear warning

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
